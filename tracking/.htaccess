RewriteEngine On

# Sirve archivos y carpetas existentes tal cual.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Permite acceder como /TOKEN y lo traduce a index.php?token=TOKEN
RewriteRule ^([^/]+)/?$ index.php?token=$1 [L,QSA]